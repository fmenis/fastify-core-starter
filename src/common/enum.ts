export enum AppMode {
  HTTP = "http",
  WORKER = "worker",
}

export enum APP_ENV {
  LOCAL = "local",
  DEVELOPMENT = "development",
  STAGING = "staging",
  PRODUCTION = "production",
}

export enum ActorType {
  USER = "USER",
  SYSTEM = "SYSTEM",
}

export enum ResourceType {
  PROFILE = "PROFILE",
}
